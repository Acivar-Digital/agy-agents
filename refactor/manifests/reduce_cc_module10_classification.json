{
  "target_files": [
    "src2/engine/module10_classification.py"
  ],
  "reference_files": [
    "src2/core/schemas/unified.py",
    "src2/engine/activity_oracle.py"
  ],
  "prompt": "You are a Staff-Level Python Engineer refactoring code to reduce cyclomatic complexity (CC).\n\n## TASK\nRefactor `classify_events` in the provided file to reduce its cyclomatic complexity from 66 to \u22645. \n\n## GOLD STANDARD EXEMPLAR (activity_oracle.py)\nI have provided `src2/engine/activity_oracle.py` as a reference file. This file was successfully refactored from CC 44 down to CC 5. \n**STUDY IT CAREFULLY.**\nNotice how it:\n1. Uses a flat orchestrator main function.\n2. Extracts complex logic into pure helpers that rely on Guard Clauses (early returns).\n3. Strictly uses Pydantic V2 schemas without raw dict literals.\nYour refactored `module10_classification.py` MUST match the architectural style, rhythm, and low-CC density of `activity_oracle.py`.\n\n## POSITIVE BLUEPRINTS (HOW TO SUCCEED)\nInstead of trying to satisfy contradictory constraints, you MUST strictly follow these 3 architectural blueprints:\n\n### 1. The Flat Orchestrator Pattern (for `classify_events`)\nYour `classify_events` function MUST look exactly like this flat sequence. Do not use complex nested loops or large if/else blocks inside it:\n```python\ndef classify_events(active_clashes: list[str], stars: list[str], raw_friction: float, ten_god_influences: dict[str, float] = None, age: int = 30) -> list[Event]:\n    events: list[Event] = []\n    events.extend(_get_health_events(active_clashes, stars, raw_friction))\n    events.extend(_get_career_events(active_clashes, stars, raw_friction))\n    events.extend(_get_wealth_events(active_clashes, stars, raw_friction, ten_god_influences))\n    events.extend(_get_relationship_events(active_clashes, stars, raw_friction))\n    \n    # Apply multipliers\n    _apply_weight_multipliers(events, age)\n    return _deduplicate_and_sort_events(events)\n```\n\n### 2. The Guard Clause Pattern (for extracted helpers)\nInside every extracted `_get_*_events` helper, you MUST use Guard Clauses (early returns) to prevent nesting. Every helper must be CC \u2264 5. Do it exactly like this:\n```python\ndef _get_health_events(active_clashes: list[str], stars: list[str], raw_friction: float) -> list[Event]:\n    # 1. GUARD CLAUSE (CC 1)\n    if \"day\" not in active_clashes and \"month\" not in active_clashes:\n        return []\n        \n    events = []\n    # 2. FLAT IF STATEMENTS (No nested elif/else pyramids)\n    if \"day\" in active_clashes:\n        events.append(Event(type=\"health\", subtype=\"chronic\", ...))\n    if \"month\" in active_clashes and raw_friction < 0:\n        events.append(Event(type=\"health\", subtype=\"acute\", ...))\n        \n    return events\n```\n\n### 3. The Tuple-of-Tuples Dispatch (for static data)\nDO NOT use dict literals `{}` and DO NOT use long `match/case` chains for data mapping (they inflate CC). Use module-level tuple-of-tuples constants for mapping weights, ages, or domain focuses:\n```python\nHEALTH_WEIGHTS = (\n    (\"general\", 25),\n    (\"chronic\", 20),\n)\ndef _get_health_weight(subtype: str) -> int:\n    return next((w for s, w in HEALTH_WEIGHTS if s == subtype), 0)\n```\n*(Note: Existing module-level dicts/lists like `FRICTION_WEALTH_GAIN` can be left alone, just don't create NEW dict literals inside your functions).* \n\n## IRONCLAD RULES\n1. **PRESERVE EXISTING HELPERS AS BLACK BOXES:** `get_event_base_weight`, `get_domain_focus_multiplier`, `_get_age_multiplier`, `get_friction_constant`, `_extract_hidden_ten_gods` must remain UNCHANGED. Do not touch their internal logic or CC.\n2. **CC AUDIT:** Count the branches (`if`, `elif`, `for`, `while`, `and`, `or`) in your generated helpers. If any helper has more than 4 branches, split it again before outputting.\n3. **NO DICT LITERALS:** You are building Event Pydantic models. Pass kwargs directly to `Event(...)`. Do not build a dict and pass it in.\n4. **SCHEMA MATCH:** Use only `type`, `subtype`, `severity`, `base_weight`, `reason`, `domain`, `probability`, `triggers` when creating `Event` instances.\n5. **NO PLACEHOLDERS:** Output the ENTIRE refactored file from top to bottom. Do NOT use `# ... rest of code ...`.\n\n## OUTPUT FORMAT \u2014 CRITICAL\nOutput ONLY the raw, executable Python code.\n- Do NOT wrap the code in markdown formatting (no ```python or ```)\n- Do NOT include any conversational text\n- The first character of your response must be the start of Python code",
  "output_dir": "refactor/output",
  "output_naming": "{stem}_refactored"
}

{
  "project_folder": "/home/yapilwsl/arthityap/baziforecaster",
  "targets": [
    "src2/engine/module1_macro.py"
  ],
  "reference_files": [
    "src2/engine/activity_oracle.py",
    "src2/core/schemas/unified.py"
  ],
  "output_dir": "refactor/output",
  "output_naming": "{stem}_refactored",
  "prompt": "You are a Staff-Level Python Engineer refactoring code to reduce cyclomatic complexity (CC).\n\n## TASK\nRefactor `calculate_macro` in the provided file to reduce its cyclomatic complexity from ~30 to ≤5.\n\nThe main function `calculate_macro` performs transit math, era block logic, and multiple formula passes. Below is the function signature and its first 40 lines of body for reference:\n\n```python\ndef calculate_macro(\n    stems: PillarMap,\n    branches: PillarMap,\n    day_stem_stream: str,\n    strength_tier: str,\n    medicine: list[str],\n    taboo: list[str],\n    neutral_elements: list[str],\n    favorable_elements: list[str],\n    unfavorable_elements: list[str],\n    da_yun_stem: str,\n    da_yun_branch: str,\n    annual_stem: str,\n    annual_branch: str,\n    target_year: int,\n    da_yun_start_year: int,\n    transit_month: str | None = None,\n) -> MacroOutput:\n\n    day_master_stem = stems.day or \"Unknown\"\nmonth_branch = branches.month or \"Unknown\"\nnatal_branches = _get_natal_branches(branches)\nyear_branch = branches.year\nty_stem = da_yun_stem or \"Jia\"\nty_branch = da_yun_branch or \"Zi\"\nmedicine_set = set(medicine)\ntaboo_set = set(taboo)\nty_stem_impact, ty_stem_combo_status = _calc_ty_stem_impact(ty_stem, day_master_stem, medicine_set, taboo_set, stems, month_branch)\nty_branch_impact = 0\nann_stem_impact = _calc_ann_stem_impact(annual_stem, medicine_set, taboo_set)\nvoid_audit = _calc_void_audit(_is_branch_void(day_stem_stream, month_branch))\nphase = _get_phase_number(target_year, da_yun_start_year)\nstem_weight = 1.3 if phase == 1 else 0.7\nbranch_weight = 0.7 if phase == 1 else 1.3\nweighted_ty_stem = int(ty_stem_impact * stem_weight)\nweighted_ty_branch = int(ty_branch_impact * branch_weight)\nera_block = _get_era_block(ty_branch, medicine, taboo, neutral_elements, favorable_elements, unfavorable_elements)\nann_element = (eb := get_branch_info(annual_branch)) and eb.element.value\nphase_of_tai_sui = get_dm_phase(_get_stem_for_element(ann_element), month_branch) if ann_element else \"Xiu\"\nseasonal_multiplier = _get_seasonal_multiplier(phase_of_tai_sui)\nannual_effect = _calc_annual_effect(seasonal_multiplier)\nann_branch_impact = 0\ntai_sui_trigger_multiplier = 1.0\nluck_harmony_multiplier = 1.0\nseasonal_multiplier_val = seasonal_multiplier\ntotal_macro_modifier = _calc_macro_modifier(weighted_ty_stem, weighted_ty_branch, ann_branch_impact, ann_stem_impact, annual_effect)\nreturn MacroOutput(\n    void_audit=void_audit,\n    seasonal_influence=MacroSeasonalInfluence(\n        decade_data=MacroDecadeData(\n            stem_impact=weighted_ty_stem,\n            branch_impact=weighted_ty_branch,\n            phase=phase,\n            climate_label=_calc_climate_label(weighted_ty_stem + weighted_ty_branch),\n        ),\n        annual_data=MacroAnnualData(\n            tai_sui_impact=0,\n            tai_sui_triggers=[],\n            stem_impact=ann_stem_impact,\n            context_label=_calc_context_label(ann_branch_impact + ann_stem_impact),\n        ),\n        era_block=era_block,\n        annual_effect_multiplier=round(annual_effect, 3),\n        TaiSui_trigger_multiplier=round(tai_sui_trigger_multiplier, 3),\n        Luck_Harmony_multiplier=round(luck_harmony_multiplier, 3),\n        Seasonal_multiplier=round(seasonal_multiplier_val, 3),\n        total_macro_modifier=total_macro_modifier,\n    ),\n)\n```\n\n## GOLD STANDARD EXEMPLAR (activity_oracle.py)\nI have provided `src2/engine/activity_oracle.py` as a reference file. This file was successfully refactored from CC 44 down to CC 5.\n**STUDY IT CAREFULLY.**\nNotice how it:\n1. Uses a flat orchestrator main function.\n2. Extracts complex logic into pure helpers that rely on Guard Clauses (early returns).\n3. Strictly uses Pydantic V2 schemas without raw dict literals.\nYour refactored `module1_macro.py` MUST match the architectural style, rhythm, and low-CC density of `activity_oracle.py`.\n\n## POSITIVE BLUEPRINTS (HOW TO SUCCEED)\nInstead of trying to satisfy contradictory constraints, you MUST strictly follow these 3 architectural blueprints:\n\n### 1. The Flat Orchestrator Pattern (for `calculate_macro`)\nYour `calculate_macro` function MUST look exactly like this flat sequence. Do not use complex nested loops or large if/else blocks inside it:\n```python\ndef calculate_macro(stems: PillarMap, branches: PillarMap, day_stem_stream: str, strength_tier: str, medicine: list[str], taboo: list[str], neutral_elements: list[str], favorable_elements: list[str], unfavorable_elements: list[str], da_yun_stem: str, da_yun_branch: str, annual_stem: str, annual_branch: str, target_year: int, da_yun_start_year: int, transit_month: str | None = None) -> MacroOutput:\n    medicine_set = set(medicine)\n    taboo_set = set(taboo)\n    day_master_stem = stems.day or \"Unknown\"\n    month_branch = branches.month or \"Unknown\"\n    natal_branches = _get_natal_branches(branches)\n    void_audit = _calc_void_audit(_is_branch_void(day_stem_stream, month_branch))\n    phase = _get_phase_number(target_year, da_yun_start_year)\n    stem_weight = 1.3 if phase == 1 else 0.7\n    branch_weight = 0.7 if phase == 1 else 1.3\n    weighted_ty_stem = int(_calc_ty_stem_impact(ty_stem, day_master_stem, medicine_set, taboo_set, stems, month_branch) * stem_weight)\n    weighted_ty_branch = int(0 * branch_weight)\n    era_block = _get_era_block(ty_branch, medicine, taboo, neutral_elements, favorable_elements, unfavorable_elements)\n    ann_stem_impact = _calc_ann_stem_impact(annual_stem, medicine_set, taboo_set)\n    ann_branch_impact = 0\n    ann_element = (eb := get_branch_info(annual_branch)) and eb.element.value\n    phase_of_tai_sui = get_dm_phase(_get_stem_for_element(ann_element), month_branch) if ann_element else \"Xiu\"\n    seasonal_multiplier = _get_seasonal_multiplier(phase_of_tai_sui)\n    annual_effect = _calc_annual_effect(seasonal_multiplier)\n    total_macro_modifier = _calc_macro_modifier(weighted_ty_stem, weighted_ty_branch, ann_branch_impact, ann_stem_impact, annual_effect)\n    return _build_macro_output(void_audit, era_block, phase, weighted_ty_stem, weighted_ty_branch, ann_stem_impact, ann_branch_impact, annual_effect, seasonal_multiplier, total_macro_modifier)\n```\n\n### 2. The Guard Clause Pattern (for extracted helpers)\nInside every extracted helper, you MUST use Guard Clauses (early returns) to prevent nesting. Every helper must be CC ≤ 5. Do it exactly like this:\n```python\ndef _calc_ty_stem_impact(ty_stem: str, day_master_stem: str, medicine_set: set[str], taboo_set: set[str], stems: PillarMap, month_branch: str) -> tuple[float, str | None]:\n    ty_stem_el = (hs := get_stem_info(ty_stem)) and hs.element.value\n    if ty_stem_el in medicine:\n        return 10.0, None\n    if ty_stem_el in taboo:\n        return -10.0, None\n    combo = get_stem_combine_element(frozenset([ty_stem, day_master_stem]))\n    if combo is None:\n        return 0.0, None\n    result_el = combo.value\n    combo_status = _get_stem_transformation_status(result_el, stems, month_branch)\n    if combo_status == \"BOUND_NO_TRANSFORM\":\n        return -2.0, None\n    impact = _calc_stem_element_impact(result_el, medicine_set, taboo_set, -5.0)\n    return impact, combo_status\n```\n\n### 3. The Tuple-of-Tuples Dispatch (for static data)\nDO NOT use dict literals `{}` and DO NOT use long `match/case` chains for data mapping (they inflate CC). Use module-level tuple-of-tuples constants for mapping weights, eras, or domain focuses:\n```python\nERA_THRESHOLDS = (\n    (\"Hostile\", 71),\n    (\"Default\", 80),\n)\ndef _get_era_ceiling(era_label: str) -> int:\n    return next((t for label, t in ERA_THRESHOLDS if label == era_label), 80)\n```\n*(Note: Existing module-level data structures can be left alone, just don't create NEW dict literals inside your functions).*\n\n## IRONCLAD RULES\n1. **PRESERVE EXISTING HELPERS AS BLACK BOXES:** `_get_natal_branches`, `_get_phase_number`, `_get_seasonal_multiplier`, `_calc_annual_effect`, `_calc_macro_modifier`, `_calc_climate_label`, `_calc_context_label`, `_is_branch_void`, `_calc_void_audit`, `_get_era_block`, `_calc_ty_stem_impact`, `_calc_ann_stem_impact`, `_get_stem_for_element`, `_get_stem_transformation_status`, `_calculate_interaction_score`, `_get_era_element`, `_get_era_label`, `_get_era_label_fallback`, `_get_era_medicine_ratio`, `_calc_stem_element_impact`, `get_priority_type`, `get_legacy_impact`, `is_harm_type`, `get_dm_phase`, `get_branch_info`, `get_stem_info`, `get_stem_combine_element`, `get_xun_kong`, `get_chong`, `get_liu_he_element`, `get_ban_he_element`, `get_po`, `get_hai` must remain UNCHANGED. Do not touch their internal logic or CC.\n2. **CC AUDIT:** Count the branches (`if`, `elif`, `for`, `while`, `and`, `or`) in your generated helpers. If any helper has more than 4 branches, split it again before outputting.\n3. **NO DICT LITERALS:** You are building Pydantic V2 models. Pass kwargs directly to `MacroOutput(...)`, `MacroSeasonalInfluence(...)`, etc. Do not build a dict and unpack it.\n4. **SCHEMA MATCH:** Use only `MacroVoidAudit`, `MacroSeasonalInfluence`, `MacroDecadeData`, `MacroAnnualData`, `MacroEraBlock`, `MacroOutput` from `src2.core.schemas.unified` when constructing output.\n5. **USE PICTLON V2 SCHEMAS MATCH/CASE WHERE APPROPRIATE:** Replace `if/elif` chains that map discrete values (like era labels, phase numbers) with `match/case` statements to reduce branching overhead. Each `match/case` arm counts as 1 branch, not N.\n6. **NO PLACEHOLDERS:** Output the ENTIRE refactored file from top to bottom. Do NOT use `# ... rest of code ...`.\n\n## OUTPUT FORMAT — CRITICAL\nOutput ONLY the raw, executable Python code.\n- Do NOT wrap the code in markdown formatting (no ```python or ```)\n- Do NOT include any conversational text\n- The first character of your response must be the start of Python code\n"
}
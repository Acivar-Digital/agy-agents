{
  "project_folder": "/home/yapilwsl/arthityap/baziforecaster",
  "targets": [
    "src2/engine/module0_geju.py"
  ],
  "reference_files": [
    "src2/engine/activity_oracle.py",
    "src2/core/schemas/unified.py"
  ],
  "prompt": "You are a Staff-Level Python Engineer refactoring code to reduce cyclomatic complexity (CC).\n\n## TASK\nRefactor the following functions in the provided file to reduce their cyclomatic complexity:\n\n1. `classify_ge_ju` — CC ~150 → target CC ≤ 5\n2. `_calculate_dominance_pct` — CC ~21 → target CC ≤ 5\n3. `_check_vibrant_structure` — CC ~20 → target CC ≤ 5\n4. `_count_ten_god_category` — CC ~16 → target CC ≤ 5\n5. `_check_counter_elements` — CC ~16 → target CC ≤ 5\n\nEach extracted helper must have CC ≤ 5.\n\n## REFERENCE IMPLEMENTATIONS (first ~30 lines of body each)\n\n### classify_ge_ju (CC ~150)\n**Signature:** `def classify_ge_ju(stems: PillarMap, branches: PillarMap, day_stem_stream: str, strength_tier: str, transformed_branches: PillarMap | None = None) -> GeJuClassificationResult:`\n**Body (first ~30 lines):**\n1:     void_branches = list(get_xun_kong(day_stem_stream) or [])\n2:     month_branch = branches.month or \"Unknown\"\n3:     trans_month_void = transformed_branches.month if transformed_branches else None\n4:     if month_branch in void_branches and trans_month_void not in (None, \"UNSTABLE\"):\n5:         pass\n6:     elif month_branch in void_branches:\n7:         trace.append(\"kong_wang_degradation\")\n8:     for _detector in (\n9:         _detect_hua_qi_ge, _detect_fei_tian_lu_ma, _detect_jian_lu_ge,\n10:         _detect_yang_ren_ge, _detect_yue_jie_ge, _detect_vibrant_structure,\n11:         _detect_cong_ge, _detect_zhuan_wang_ge, _detect_ten_god_pattern,\n12:     ):\n13:         _result = _detector(stems, branches, day_stem_stream, strength_tier, transformed_branches, trace)\n14:         if _result is not None:\n15:             return _result\n16:     _pat = _safe_ge_ju_pattern(\"common_pattern\")\n17:     return GeJuClassificationResult(ge_ju=_pat, pattern_name=_pat.name, bonus=_pat.bonus, pattern_key=\"common_pattern\", calculation_trace=trace)\n\n### _calculate_dominance_pct (CC ~21)\n**Signature:** `def _calculate_dominance_pct(stems: PillarMap, branches: PillarMap, transformed_branches: PillarMap | None = None) -> tuple[ElementMap, str, float]:`\n**Body (first ~30 lines):**\n1:     counts = ElementMap()\n2:     total = 0.0\n3:\n4:     def add_count(el: str, val: float):\n5:         nonlocal total\n6:         if el == \"Wood\": counts.wood += val\n7:         elif el == \"Fire\": counts.fire += val\n8:         elif el == \"Earth\": counts.earth += val\n9:         elif el == \"Metal\": counts.metal += val\n10:         elif el == \"Water\": counts.water += val\n11:         total += val\n12:\n13:     for p_name in [\"year\", \"month\", \"day\", \"hour\"]:\n14:         stem = getattr(stems, p_name)\n15:         if stem:\n16:             hs = get_stem_info(stem)\n17:             if hs and hs.element:\n18:                 add_count(hs.element.value, 1.0)\n19:\n20:         branch = getattr(branches, p_name)\n21:         if branch:\n22:             _trans = getattr(transformed_branches, p_name) if transformed_branches else None\n23:             if _trans and _trans != \"UNSTABLE\":\n24:                 add_count(_trans, 1.0)\n25:             elif _trans == \"UNSTABLE\":\n26:                 eb = get_branch_info(branch)\n27:                 if eb and eb.element:\n28:                     add_count(eb.element.value, 1.0)\n29:\n30:                 for _e in get_zhi_hidden(branch):\n31:                     v = get_stem_info(_e.stem.label)\n32:                     if v and v.element:\n33:                         add_count(v.element.value, 0.3)\n34:             else:\n35:                 eb = get_branch_info(branch)\n36:                 if eb and eb.element:\n37:                     add_count(eb.element.value, 1.0)\n38:\n39:                 for _e in get_zhi_hidden(branch):\n40:                     v = get_stem_info(_e.stem.label)\n41:                     if v and v.element:\n42:                         add_count(v.element.value, 0.3)\n43:\n44:     if total == 0:\n45:         return counts, \"\", 0.0\n46:\n47:     items = [\n48:         (\"Wood\", counts.wood),\n49:         (\"Fire\", counts.fire),\n50:         (\"Earth\", counts.earth),\n51:         (\"Metal\", counts.metal),\n52:         (\"Water\", counts.water),\n53:     ]\n54:     dominant, dominant_val = max(items, key=lambda x: x[1])\n55:     dominant_pct = dominant_val / total\n56:\n57:     return counts, dominant, dominant_pct\n\n### _check_vibrant_structure (CC ~20)\n**Signature:** `def _check_vibrant_structure(stems: PillarMap, branches: PillarMap, month_branch: str, transformed_branches: PillarMap | None = None) -> str | None:`\n**Body (first ~30 lines):**\n1:     natal_branches = set()\n2:     natal_stems = set()\n3:     for p_name in [\"year\", \"month\", \"day\", \"hour\"]:\n4:         b = getattr(branches, p_name)\n5:         if b:\n6:             natal_branches.add(b)\n7:         s = getattr(stems, p_name)\n8:         if s:\n9:             natal_stems.add(s)\n10:\n11:     for pattern_key in [\"qu_zhi_ge\", \"yan_shang_ge\", \"run_xia_ge\", \"cong_ge_ge\", \"jia_se_ge\"]:\n12:         req_element, req_stems = get_vibrant_requirements(pattern_key)\n13:         has_branches = False\n14:         for option in get_vibrant_branch_options(pattern_key):\n15:             if option.issubset(natal_branches):\n16:                 option_valid = True\n17:                 for opt_branch in option:\n18:                     _trans = None\n19:                     for p_name in [\"year\", \"month\", \"day\", \"hour\"]:\n20:                         if getattr(branches, p_name) == opt_branch and transformed_branches:\n21:                             _trans = getattr(transformed_branches, p_name)\n22:                     if _trans == \"UNSTABLE\":\n23:                         option_valid = False\n24:                         break\n25:                     if _trans and _trans != req_element:\n26:                         option_valid = False\n27:                         break\n28:                 if option_valid:\n29:                     has_branches = True\n30:                     break\n\n### _count_ten_god_category (CC ~16)\n**Signature:** `def _count_ten_god_category(dm_stem: str, category_set: set, stems: PillarMap, branches: PillarMap, transformed_branches: PillarMap | None = None) -> int:`\n**Body (first ~30 lines):**\n1:     positions = []\n2:     for p_name in [\"year\", \"month\", \"day\", \"hour\"]:\n3:         stem = getattr(stems, p_name)\n4:         if stem:\n5:             positions.append(stem)\n6:         branch = getattr(branches, p_name)\n7:         if branch:\n8:             _trans = getattr(transformed_branches, p_name) if transformed_branches else None\n9:             if _trans and _trans != \"UNSTABLE\":\n10:                 b_info = get_branch_info(branch)\n11:                 _polarity = b_info.polarity.value if b_info else \"Yang\"\n12:                 _stem_for_el = get_stem_for_element(_trans, _polarity)\n13:                 positions.append(_stem_for_el)\n14:             elif _trans == \"UNSTABLE\":\n15:                 for _entry in get_zhi_hidden(branch):\n16:                     if _entry.stem.label:\n17:                         positions.append(_entry.stem.label)\n18:             else:\n19:                 main_qi = get_zhi_hidden(branch)\n20:                 if main_qi:\n21:                     positions.append(main_qi[0].stem.label)\n22:     count = 0\n23:     for s in positions:\n24:         if s and s != dm_stem:\n25:             tg = get_ten_god(dm_stem, s)\n26:             if tg in category_set:\n27:                 count += 1\n28:     return count\n\n### _check_counter_elements (CC ~16)\n**Signature:** `def _check_counter_elements(stems: PillarMap, branches: PillarMap, dominant_element: str, transformed_branches: PillarMap | None = None) -> bool:`\n**Body (first ~30 lines):**\n1:     counter_el = get_controller(dominant_element)\n2:     if not counter_el:\n3:         return False\n4:\n5:     for p_name in [\"year\", \"month\", \"day\", \"hour\"]:\n6:         stem = getattr(stems, p_name)\n7:         if stem and (v := get_stem_info(stem)) and v.element.value == counter_el:\n8:             return True\n9:\n10:         branch = getattr(branches, p_name)\n11:         if branch:\n12:             _trans = getattr(transformed_branches, p_name) if transformed_branches else None\n13:             if _trans == counter_el:\n14:                 return True\n15:             elif _trans != \"UNSTABLE\" and _trans is None:\n16:                 if (v := get_branch_info(branch)) and v.element.value == counter_el:\n17:                     return True\n18:                 for entry in get_zhi_hidden(branch):\n19:                     if (v := get_stem_info(entry.stem.label)) and v.element.value == counter_el:\n20:                         return True\n21:\n22:     return False\n\n## GOLD STANDARD EXEMPLAR (activity_oracle.py)\nI have provided `src2/engine/activity_oracle.py` as a reference file. This file was successfully refactored from CC 44 down to CC 5.\n**STUDY IT CAREFULLY.**\nNotice how it:\n1. Uses a flat orchestrator main function.\n2. Extracts complex logic into pure helpers that rely on Guard Clauses (early returns).\n3. Strictly uses Pydantic V2 schemas without raw dict literals.\nYour refactored `module0_geju.py` MUST match the architectural style, rhythm, and low-CC density of `activity_oracle.py`.\n\n## POSITIVE BLUEPRINTS (HOW TO SUCCEED)\nInstead of trying to satisfy contradictory constraints, you MUST strictly follow these 3 architectural blueprints:\n\n### 1. The Flat Orchestrator Pattern (for `classify_ge_ju`)\nYour `classify_ge_ju` function MUST look exactly like this flat sequence. Do not use complex nested loops or large if/else blocks inside it:\n```python\ndef classify_ge_ju(stems: PillarMap, branches: PillarMap, day_stem_stream: str, strength_tier: str, transformed_branches: PillarMap | None = None) -> GeJuClassificationResult:\n    trace: list[str] = []\n    void_branches = list(get_xun_kong(day_stem_stream) or [])\n    month_branch = branches.month or \"Unknown\"\n    trans_month_void = transformed_branches.month if transformed_branches else None\n    if month_branch in void_branches and trans_month_void not in (None, \"UNSTABLE\"):\n        pass\n    elif month_branch in void_branches:\n        trace.append(\"kong_wang_degradation\")\n    for _detector in (\n        _detect_hua_qi_ge, _detect_fei_tian_lu_ma, _detect_jian_lu_ge,\n        _detect_yang_ren_ge, _detect_yue_jie_ge, _detect_vibrant_structure,\n        _detect_cong_ge, _detect_zhuan_wang_ge, _detect_ten_god_pattern,\n    ):\n        _result = _detector(stems, branches, day_stem_stream, strength_tier, transformed_branches, trace)\n        if _result is not None:\n            return _result\n    _pat = _safe_ge_ju_pattern(\"common_pattern\")\n    return GeJuClassificationResult(ge_ju=_pat, pattern_name=_pat.name, bonus=_pat.bonus, pattern_key=\"common_pattern\", calculation_trace=trace)\n```\n\n### 2. The Guard Clause Pattern (for extracted helpers)\nInside every extracted `_detect_*` or `_check_*` helper, you MUST use Guard Clauses (early returns) to prevent nesting. Every helper must be CC ≤ 5. Do it exactly like this:\n```python\ndef _detect_fei_tian_lu_ma(dm_stem: str, branches: PillarMap, trace: list[str]) -> GeJuClassificationResult | None:\n    # GUARD CLAUSE 1 (CC 1)\n    if dm_stem != \"Geng\" and dm_stem != \"Ren\":\n        return None\n    # Guard clause for branch count checks\n    if dm_stem == \"Geng\":\n        if _count_branches(branches, \"Zi\") < 2:\n            return None\n        if _count_branches(branches, \"Wu\") > 0:\n            return None\n        pat = _safe_ge_ju_pattern(\"fei_tian_lu_ma\")\n        return GeJuClassificationResult(ge_ju=pat, pattern_name=pat.name if pat else \"\", bonus=pat.bonus if pat else 0, pattern_key=\"fei_tian_lu_ma\", calculation_trace=trace)\n    # Guard clause for Ren path\n    if _count_branches(branches, \"Wu\") < 2:\n        return None\n    if _count_branches(branches, \"Zi\") > 0:\n        return None\n    pat = _safe_ge_ju_pattern(\"fei_tian_lu_ma\")\n    return GeJuClassificationResult(ge_ju=pat, pattern_name=pat.name if pat else \"\", bonus=pat.bonus if pat else 0, pattern_key=\"fei_tian_lu_ma\", calculation_trace=trace)\n```\n\n### 3. The Tuple-of-Tuples Dispatch (for static data)\nDO NOT use dict literals `{}` and DO NOT use long `match/case` chains for data mapping (they inflate CC). Use module-level tuple-of-tuples constants for mapping weights, patterns, or domain lookups:\n```python\nVIBRANT_REQUIREMENTS = (\n    (\"qu_zhi_ge\", \"Wood\", {\"Jia\", \"Yi\"}),\n    (\"yan_shang_ge\", \"Fire\", {\"Bing\", \"Ding\"}),\n)\ndef _get_vibrant_req(pattern_key: str) -> tuple[str, set[str]]:\n    return next((el, st) for k, el, st in VIBRANT_REQUIREMENTS if k == pattern_key)\n```\n*(Note: Existing module-level dicts/lists like `FRICTION_WEALTH_GAIN` can be left alone, just don't create NEW dict literals inside your functions).*\n\n## IRONCLAD RULES\n1. **PRESERVE EXISTING HELPERS AS BLACK BOXES:** `_has_meaningful_root`, `_is_dm_weak`, `_count_branch_roots`, `_detect_hua_qi_ge`, `_detect_fei_tian_lu_ma`, `_detect_jian_lu_ge`, `_detect_yang_ren_ge`, `_detect_yue_jie_ge`, `_classify_cong_ge_key`, `_detect_cong_ge`, `_detect_zhuan_wang_ge`, `_detect_ten_god_pattern`, `_detect_vibrant_structure`, `_safe_ge_ju_pattern`, `_select_month_ten_god`, `_apply_ten_god_overrides`, `get_stem_for_element`, `get_vibrant_requirements`, `get_vibrant_branch_options`, `get_suppressive_stems`, `get_shattering_branches`, `get_pattern_for_ten_god`, `get_tier_base`, `get_controller`, `get_producer`, `_count_branches`, `compute_ge_ju_alignment_mod`, `validate_special_structure` must remain UNCHANGED. Do not touch their internal logic or CC.\n2. **CC AUDIT:** Count the branches (`if`, `elif`, `for`, `while`, `and`, `or`) in your generated helpers. If any helper has more than 4 branches, split it again before outputting.\n3. **NO DICT LITERALS:** You are building Pydantic models and domain lookups. Do not create new dict literals inside your functions. Use tuple-of-tuples constants or Pydantic schemas instead.\n4. **SCHEMA MATCH:** Use only the existing Pydantic V2 schemas (`GeJuClassificationResult`, `GeJuPattern`, `PillarMap`, `ElementMap`, `Element`, `HiddenStemEntry`) when constructing return values. Pass kwargs directly to the model constructors.\n5. **NO PLACEHOLDERS:** Output the ENTIRE refactored file from top to bottom. Do NOT use `# ... rest of code ...`.\n6. **MATCH/CASE DISPATCH:** Use `match/case` where appropriate for control flow dispatch (e.g., stem-to-element mapping) — it keeps helpers flat and CC low.\n\n## OUTPUT FORMAT — CRITICAL\nOutput ONLY the raw, executable Python code.\n- Do NOT wrap the code in markdown formatting (no ```python or ```)\n- Do NOT include any conversational text\n- The first character of your response must be the start of Python code",
  "output_dir": "refactor/output",
  "output_naming": "{stem}_refactored"
}
{
  "project_folder": "/home/yapilwsl/arthityap/baziforecaster",
  "targets": [
    "src2/engine/contradiction_resolver.py"
  ],
  "reference_files": [
    "src2/engine/activity_oracle.py",
    "src2/core/schemas/unified.py"
  ],
  "output_dir": "refactor/output",
  "output_naming": "{stem}_refactored",
  "prompt": "You are a Staff-Level Python Engineer refactoring code to reduce cyclomatic complexity (CC).\n\n## TASK\nRefactor two functions in the provided file to reduce their cyclomatic complexity:\n- _extract_signals (CC ~19) → target CC ≤ 5\n- classify_true_vs_apparent (CC ~17) → target CC ≤ 5\n\nCRITICAL: resolve_contradictions (CC 43) is a BLACK BOX — do NOT modify it, do NOT touch it, do NOT refactor its internals. Preserve it exactly as-is.\n\n## GOLD STANDARD EXEMPLAR (activity_oracle.py)\nI have provided src2/engine/activity_oracle.py as a reference file. This file was successfully refactored from CC 44 down to CC 5.\nSTUDY IT CAREFULLY. Notice how it:\n1. Uses a flat orchestrator main function.\n2. Extracts complex logic into pure helpers that rely on Guard Clauses (early returns).\n3. Strictly uses Pydantic V2 schemas without raw dict literals.\nYour refactored contradiction_resolver.py MUST match the architectural style, rhythm, and low-CC density of activity_oracle.py.\n\n## POSITIVE BLUEPRINTS (HOW TO SUCCEED)\nInstead of trying to satisfy contradictory constraints, you MUST strictly follow these 4 architectural blueprints:\n\n### 1. The Flat Orchestrator Pattern\nThe refactored resolve_contradictions (BLACK BOX, preserved) should orchestrate helpers that are flat and sequential. Any NEW helpers you extract must follow this pattern:\n\n\n### 2. The Guard Clause Pattern (for extracted helpers)\nInside every extracted helper, use Guard Clauses (early returns) to prevent nesting. No helper should exceed CC 5.\n\n### 3. Tuple Dispatch for Branching\n Replace if/elif chains that dispatch on string/type values with tuple-dispatch or dict-based lookup tables. Use Pydantic models or Enums instead of raw dict literals for domain lookups.\n\n### 4. No Dict Literals for Domain Lookups\nReplace inline dict literals like {\"a\": 1, \"b\": 2} with Pydantic V2 schemas or constant lookups defined at module level. The only exception is small, static, non-domain dicts used purely for configuration mapping (but prefer Pydantic models).\n\n## REFERENCE FILES\n- activity_oracle.py: Few-shot exemplar showing flat orchestrator + guard clause helpers.\n- unified.py: Pydantic V2 schemas — use these for data modeling instead of dict literals.\n\n## TARGET FUNCTIONS (signatures + first 30 lines of body shown for context)\n\n### _extract_signals (target CC ≤ 5)\n\nThis function currently has CC ~19 due to multiple nested conditionals for ge_ju, strength, disruptors, macro, and ten_gods signal extraction. All branches append to the signals list with different conditional logic. Extract each signal-source branch into its own pure helper function with guard clauses so each helper stays at CC ≤ 5.\n\n### classify_true_vs_apparent (target CC ≤ 5)\n\nThis function currently has CC ~17 due to nested conditionals checking temporal windows, element production relationships, and signal directions. Extract each classification axis into a separate pure helper that returns a partial result, then compose the final classification in a flat orchestrator.\n\n## CONSTRAINTS\n- Output raw Python only. No markdown, no explanations, no comments in the output beyond what is needed for the code itself.\n- Each helper function must have CC ≤ 5.\n- No dict literals for domain lookups (use Pydantic V2 schemas, Enums, or module-level constant registries).\n- All domain data that was stored in dict literals (e.g., SAN_HE_PEAK, spec_map, rank maps, window definitions) must be converted into Pydantic models or constant tuple/list lookups.\n- Preserve all existing imports and type annotations.\n- Preserve resolve_contradictions as a BLACK BOX — do not modify its body at all.\n- The refactored file must pass ruff check with zero violations."
}
{
  "project_folder": "/home/yapilwsl/arthityap/baziforecaster",
  "targets": [
    "src2/engine/narrative_simplifier.py"
  ],
  "reference_files": [
    "src2/engine/activity_oracle.py",
    "src2/core/schemas/unified.py"
  ],
  "prompt": "You are a Staff-Level Python Engineer refactoring code to reduce cyclomatic complexity (CC).\n\n## TASK\nRefactor `simplify_advisory` in `src2/engine/narrative_simplifier.py` to reduce its CC from ~16 to ≤5.\n\n## GOLD STANDARD EXEMPLAR (activity_oracle.py)\nStudy `src2/engine/activity_oracle.py` — it was refactored from CC 44→5. Notice the flat orchestrator + guard clause + tuple dispatch patterns.\n\n## POSITIVE BLUEPRINTS\n\n### Flat Orchestrator Pattern\n`simplify_advisory` delegates to per-mode and per-language helpers:\n```python\nasync def simplify_advisory(text, alias, events, language, mode):\n    if not text:\n        return text\n    mode_handler = _get_mode_handler(mode)\n    simplified = await mode_handler(text, alias, events)\n    localized = await _localify(simplified, language)\n    return _format_output(localized, mode)\n```\n\n### Guard Clause Pattern\nEvery helper uses early returns:\n```python\ndef _get_mode_handler(mode: str):\n    match mode:\n        case \"ORACLE\":\n            return _oracle_handler\n        case \"DAILY\" | _:\n            return _daily_handler\n```\n\n### Mode Dispatching\nUse match/case for mode routing instead of nested if/elif:\n```python\ndef _format_output(text, mode):\n    match mode:\n        case \"ORACLE\":\n            return f\"## {text}\"\n        case \"DAILY\" | _:\n            return text\n```\n\n## IRONCLAD RULES\n1. CC ≤5 per helper function — split anything above\n2. No raw dict literals for domain lookups — use match/case or tuple-of-tuples\n3. Preserve all existing helper functions as BLACK BOXES (get_month_simplifier_agent, get_advisory_simplifier_agent, LazyAgentProxy, simplify_month_narrative, prompt constants)\n4. Preserve all existing Pydantic return types\n5. Preserve all function signatures\n6. No invented Pydantic models\n7. Output ONLY raw Python code. No markdown fences, no prose.",
  "output_dir": "refactor/output",
  "output_naming": "{stem}_refactored"
}
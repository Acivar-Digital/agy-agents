{
  "project_folder": "/home/yapilwsl/arthityap/baziforecaster",
  "targets": [
    "src2/engine/chart_generator.py"
  ],
  "reference_files": [
    "src2/engine/activity_oracle.py",
    "src2/core/schemas/unified.py"
  ],
  "prompt": "You are a Staff-Level Python Engineer refactoring code to reduce cyclomatic complexity (CC).\n\n## TASK\nRefactor `generate_7day_chart` in `src2/engine/chart_generator.py` to reduce its CC from 15 to ≤5.\n\n## GOLD STANDARD EXEMPLAR (activity_oracle.py)\nStudy `src2/engine/activity_oracle.py` — it was refactored from CC 44→5. Notice the flat orchestrator + guard clause + tuple dispatch patterns.\n\n## POSITIVE BLUEPRINTS\n\n### Flat Orchestrator Pattern\n`generate_7day_chart` delegates to per-step helpers:\n```python\ndef generate_7day_chart(records: list[DailyForecastRecord], output_path: str) -> str:\n    if not records:\n        raise ValueError(\"No daily forecast records provided for chart generation\")\n    records = records[:7]\n    dates, scores = _process_records(records)\n    chart = _build_chart(dates, scores)\n    _save_chart(chart, output_path)\n    return output_path\n```\n\n### Guard Clause Pattern\nEvery helper uses early returns:\n```python\ndef _process_records(records: list[DailyForecastRecord]) -> tuple[list[str], list[float]]:\n    if not records:\n        return [], []\n    dates = [r.date for r in records]\n    scores = [r.score for r in records]\n    return dates, scores\n```\n\n## IRONCLAD RULES\n1. CC ≤5 per helper function — split anything above\n2. No raw dict literals for domain lookups — use tuple-of-tuples or match/case\n3. Preserve all existing Pydantic return types\n4. Preserve all function signatures\n5. No invented Pydantic models\n6. Output ONLY raw Python code. No markdown fences, no prose.",
  "output_dir": "refactor/output",
  "output_naming": "{stem}_refactored"
}